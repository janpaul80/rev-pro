server {
    listen 80;
    server_name rev-pro.dev www.rev-pro.dev;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 "Bootstrap mode: Waiting for Certbot SSL... Refresh in 2 minutes.";
    }
}
